# Copy this file to .env and adjust if needed.
# Matches the credentials in docker-compose.yml — do not put real/production
# secrets in this example file.
DATABASE_URL="postgresql://mateka:mateka_dev_password@localhost:5432/mateka?schema=public"
